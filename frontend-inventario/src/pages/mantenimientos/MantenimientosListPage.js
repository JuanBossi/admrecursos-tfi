export default function MantenimientosListPage() {
  // Cuando expongas endpoints de mantenimiento, acá metemos tabla+filtros
  return (
    <div className="card">
      <h3>Mantenimientos</h3>
      <p>Pendiente de integrar con el backend (PROGRAMADO / próximos N días).</p>
    </div>
  );
}
