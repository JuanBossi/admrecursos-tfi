/**
 * @template T
 * @typedef {Object} Paginated
 * @property {T[]} items
 * @property {number} total
 * @property {number} page
 * @property {number} limit
 */

/**
 * @template T
 * @typedef {Object} ApiResponse
 * @property {boolean} ok
 * @property {T} data
 */

// Export vacío para poder importar el módulo (y que JSDoc funcione con import())
export {};
